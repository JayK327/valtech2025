server:
  port: 9070

spring:
  application:
    name: Assignment-MS-result-service

  datasource:
    url: jdbc:postgresql://localhost:5432/microser
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka